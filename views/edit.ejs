<!DOCTYPE html>
<html>
<head>
  <title>Edit Password</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h3>Edit Password</h3>
    <form action="/passwords/update/<%= password._id %>" method="POST" class="card p-4 shadow-sm">
      <div class="mb-3">
        <input type="text" name="website" class="form-control" value="<%= password.website %>" required>
      </div>
      <div class="mb-3">
        <input type="text" name="username" class="form-control" value="<%= password.username %>" required>
      </div>
      <div class="mb-3">
        <input type="text" name="password" class="form-control" value="<%= password.password %>" required>
      </div>
      <button type="submit" class="btn btn-success">Update</button>
      <a href="/passwords" class="btn btn-secondary ms-2">Cancel</a>
    </form>
  </div>
</body>
</html>
